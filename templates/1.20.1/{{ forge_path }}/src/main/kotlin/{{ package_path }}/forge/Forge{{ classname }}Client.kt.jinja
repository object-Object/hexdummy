package {{ package }}.forge

import {{ package }}.{{ classname }}Client
import {{ package }}.config.{{ classname }}Config
import net.minecraftforge.client.ConfigScreenHandler.ConfigScreenFactory
import net.minecraftforge.fml.event.lifecycle.FMLClientSetupEvent
import thedarkcolour.kotlinforforge.forge.LOADING_CONTEXT

object Forge{{ classname }}Client {
    fun init(event: FMLClientSetupEvent) {
        {{ classname }}Client.init()
        LOADING_CONTEXT.registerExtensionPoint(ConfigScreenFactory::class.java) {
            ConfigScreenFactory { _, parent -> {{ classname }}Config.getConfigScreen(parent) }
        }
    }
}
